import React, { useState } from "react";
import "../css/EnrollFile.css"
export const EnrollFile = (props)=> {
  const {storeName, storeData} = props
  return (
    <div className="choose">

    </div>
  )
}